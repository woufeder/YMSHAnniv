# YMSH Anniv 流程測試手冊

本手冊給流程測試人員使用。專案為靜態 GitHub Pages 網站；請以正常瀏覽器操作，不需要開啟 DevTools、修改網址參數或手動調整儲存資料。

## 測試前

1. 從首頁開始。
2. 按下首頁的「清空所有紀錄」，在確認視窗中同意清除後，等待頁面重新整理。
3. 每一條獨立測試流程都應先清除紀錄，避免已看過的對話、遊戲紀錄或成就影響結果。
4. 請確認瀏覽器可以播放音效；首次播放可能需要先點擊頁面。

> 清除紀錄會一併移除姓名、角色、遊戲進度、成就、設定與美術作品，這是預期行為。

## 首頁與身分判定

### 共通檢查

- 首頁姓名輸入框應完整顯示、可以輸入中文與英文，不能縮成小方格。
- 按「進入校門」後，應先播放校門口對話，再顯示地圖。
- 首頁左上資訊圖示應前往「素材標示」頁面，不應再出現 Popover。
- 素材標示頁的返回箭頭應回到上一頁；直接開啟該頁、沒有上一頁時才會回到地圖。

### 三種文本身分

請各測一次，並至少確認校門口、教室、花圃、實驗室、藝術大樓、穿堂與校長室的前置對話有切換成正確版本。

| 測試身分 | 輸入方式 | 預期結果 |
| --- | --- | --- |
| 學生 | 輸入 `data/member.js` 名單中的學生姓名或別名 | 以 `student` 對話與校長室學生文本呈現。 |
| 老師 | 輸入 `data/member.js` 名單中的老師姓名或別名 | 以 `teacher` 對話與校長室老師文本呈現。 |
| 訪客 | 輸入不在名單中的任意姓名 | 以預設訪客文本呈現，校長室使用預設文本。 |

### 隱藏輸入

- 分別輸入 `桓成康`、`校長`。
- 預期：不會進入地圖，輸入框清空並顯示提示，同時解鎖 `fakerplayer`（`I can see you`）成就 Toast。
- 此測試完成後請清除紀錄，再繼續其他流程。

## 共通介面與對話

- 每一頁都應有設定按鈕；調整 BGM 音量與文字速度後，切換頁面仍要保留設定。
- 所有對話頁左上皆使用同一種返回箭頭，應回到地圖。
- 對話文字中的等待、音效、背景切換與角色圖效果應能正常播放，不應讓遊戲 UI 在前置對話結束前露出。
- 每個地點的前置對話首次進入時播放；同一輪測試再次進入則略過，直接進入主要內容。
- 首頁「清空所有紀錄」後，前置對話應重新播放。

## 地圖

- 點擊各地點 Pin 應前往正確頁面：藝術大樓、教室、花圃、實驗室、校長室、穿堂、操場。
- 左上只顯示六項隱藏成就：未達成不顯示，達成後顯示「達成成就：名稱」。
- 遊戲完成紀錄會供校長室生成紀念卡使用，請在校長室確認小遊戲數量。

## 小遊戲與成就

| 地點／遊戲 | 正常完成條件 | 對應紀錄 | 成就測試 |
| --- | --- | --- | --- |
| 教室／快問快答 | 隨機抽出的 10 題全部作答完畢。 | 小遊戲數量加 1。 | 10 題全對、100 分，解鎖 `classroom-perfect`（`滿分 Pass`）。 |
| 花圃／花園急救站 | 60 秒結束時健康值大於 0。工具可用按鈕或數字 `1` 至 `4` 切換。 | 小遊戲數量加 1。 | 全程維持 5 顆心，解鎖 `garden-perfect`。 |
| 實驗室／實驗紀錄校對 | 配對完 8 組資料，完成 4 x 4 翻牌。 | 小遊戲數量加 1。 | 結算等級為 **S** 或 **A** 時，解鎖 `lab-grade-a`；B、C 不應解鎖。 |
| 操場／圍捕小狗大作戰 | 30 秒內依提示按對方向鍵或畫面按鈕，累積 20 次成功口令。 | 小遊戲數量加 1。 | 成功抓到小狗後解鎖 `playground-dog`，並播放「主人認領」結局對話。 |
| 音樂教室／校歌演奏 | 依畫面樂譜從頭到尾按對音符；按錯不會重置進度。 | 不計入四個小遊戲數量。 | 完整演奏後解鎖 `school-song`。 |
| 美術教室 | 繪製任意內容後按「儲存作品」，再前往校長室。 | 不計入四個小遊戲數量。 | 無隱藏成就；作品應出現在校長室與下載紀念卡。 |

### 遊戲流程重點

- 教室、花圃、實驗室完成後可重新挑戰或返回地圖；重新挑戰應從初始分數／生命／牌組開始。
- 操場第一次進入先播放前置對話，再顯示遊戲說明 Modal。失敗結局的選項應可「再試一次」或「返回地圖」；成功結局對話點完後回地圖。
- 花圃事件使用錯誤工具或放置不處理會扣心；工具音效應分別對應澆水、陽光、施肥、整理。
- 實驗室配對正確／錯誤都應播放相對應音效；翻牌時卡片內容不可超出卡片邊界。
- 操場與花圃都有環境音，且會和 BGM 同時播放；進入音樂教室時 BGM 應靜音，離開後恢復使用者設定音量。
- 美術教室應可切換筆刷、顏色、筆刷大小、油漆桶、復原、重做與清除；畫布不得在直向手機、平板或 125% 縮放時超出畫面。

## 六項成就總驗證

完成六項成就後，請回到地圖與校長室確認：

1. `fakerplayer`（`I can see you`）：輸入禁止名稱。
2. `school-song`：音樂教室完整演奏。
3. `playground-dog`：30 秒內成功抓到小狗。
4. `garden-perfect`：完成時保有 5 顆心。
5. `classroom-perfect`（`滿分 Pass`）：教室 10 題全對。
6. `lab-grade-a`：實驗室結算等級為 S 或 A。

每次第一次達成時，右下應出現成就 Toast，內容包含成就名稱與達成原因，並播放成就音效。已達成同一項後重複完成，不應重複跳出 Toast。

## 藝術大樓與美術作品

- 藝術大樓首次進入先播放對話，結束後才可選擇音樂教室或美術教室。
- 音樂教室返回藝術大樓與返回地圖按鈕皆應可使用。
- 美術教室儲存後，離開再回來仍應保留作品；按返回藝術大樓或返回地圖也會先自動儲存。
- 校長室顯示的美術作品，筆畫位置與美術教室畫布應一致，不能因縮放而裁切或偏移。

## 穿堂留言板

- 留言便條紙應以不同顏色、角度與位置呈現；可以輕微重疊，但不應全部集中在同一處。
- 便條紙預覽需保留換行、限制固定高度，內容過長以 `...` 提示。
- 點擊便條紙後，Modal 應使用同色系、顯示完整訊息與中文日期格式，並保留原有換行。
- 新增留言時：姓名與祝福訊息皆為必填；祝福訊息上限 500 字，字數計數器必須即時更新。
- 成功送出後，首頁玩家姓名不可被留言姓名覆寫。

## 校長室紀念卡

- 校長室首次進入應先播放與玩家身分對應的對話。
- 「更新紀念卡」後，卡片標題須使用目前玩家姓名，並顯示四個小遊戲數量與六個成就數量。
- 文章內容應隨學生／老師／訪客、完成的小遊戲、成就與美術作品變化；成就文字應接續對應遊戲段落，不應額外斷成不相關段落。
- 網頁版文章左頁最多放 6 個段落，其餘段落放右頁；文字少時可只顯示左頁內容。美術作品固定放右頁。
- 日期應為「YYYY年M月D日．永明高中校長室」，並固定在右頁底部。
- 下載紀念卡應完整輸出雙頁版面、校徽底圖、文章、美術作品與日期；不得裁切、留白框或文字超出頁面。

## 素材標示與設定

- 素材標示頁須讀取 `data/credits.json`，分類按鈕可切換，清單區可獨立捲動，整個網頁本身不應出現頁面捲軸。
- 「聯絡與聲明」需顯示使用聲明與可維護的聯絡方式欄位。
- 素材清單單筆格式應為：名稱、類型、創作者與來源並列、備註；不應出現分隔線。
- 設定視窗內的「製作資訊・素材來源」也應前往素材標示頁；從 `games/` 內頁開啟時路徑必須正確。

## 回報格式

請每個問題附上以下資訊：

1. 測試身分：學生／老師／訪客。
2. 操作路徑：例如「首頁 → 地圖 → 花圃 → 開始遊戲」。
3. 預期結果與實際結果。
4. 發生頻率：每次、偶發或僅特定裝置／縮放比例。
5. 螢幕截圖或錄影；若是聲音問題，請說明是否已先點擊過頁面。
