# YMSH Anniv 流程測試手冊

本文件供流程測試人員使用。請以部署後的 GitHub Pages 網址進行測試，不需也不應啟動 PHP 或其他後端服務。

## 測試前準備

每個獨立案例開始前，請在瀏覽器開發者工具的 **Application / 儲存空間** 清除本站資料，或在 Console 執行：

```js
localStorage.clear();
location.reload();
```

這會同時清除遊戲進度、成就、對話首次觀看紀錄、姓名與設定。若只想確認目前狀態，可執行：

```js
console.table(Object.entries(localStorage));
```

## 入口與角色

| 案例 | 操作 | 預期結果 |
| --- | --- | --- |
| 學生 | 輸入 `data/member.js` 中一位學生資料的姓名或別名 | 進入地圖；後續讀取 `student` 對話與紀念卡文本。 |
| 教師 | 輸入 `data/member.js` 中一位教師資料的姓名或別名 | 進入地圖；後續讀取 `teacher` 對話與紀念卡文本。 |
| 訪客 | 輸入未列在成員資料中的一般姓名 | 進入地圖；對話與紀念卡使用 `default` 文本。 |
| 偽裝校長 | 輸入 `桓成康` 或 `校長` | 不可進入地圖、輸入框會清空並出現提示；取得 `fakerplayer` 隱藏成就 Toast。之後以正常姓名重新進入。 |

確認方式：Console 中的 `playerRole` 應分別為 `student`、`teacher` 或 `guest`。校長室會將非 `student` / `teacher` 的角色視為 `default`。

## 對話與地圖

請至少以「學生、教師、訪客」三種身分各跑一次下列項目：

- 首次進入地圖會播放入口對話；重新整理或返回後不應重複播放首次對話。
- 所有地點的對話角色圖、名稱、背景切換、等待效果、選項與下一步按鈕正常顯示。
- 有 `student`、`teacher`、`default` 分支的 JSON，應依輸入角色顯示對應文本；沒有該角色分支時應安全回退到 `default`。
- 左上返回地圖按鈕、右上設定按鈕可使用；返回/前進瀏覽器歷史後 BGM 仍能恢復。
- 完成遊戲後回地圖，對應地圖 Pin 應顯示完成狀態。

## 小遊戲與完成狀態

| 地點 / 遊戲 | 一般完成條件 | 完成後確認 |
| --- | --- | --- |
| 教室快問快答 | 完成 10 題作答 | 地圖教室 Pin 變為完成；`completedGames` 包含 `classroom`。 |
| 花圃急救站 | 在 1 分鐘內完成花圃急救 | 地圖花圃 Pin 變為完成；`completedGames` 包含 `garden`。 |
| 實驗紀錄校對 | 完成 4 x 4 翻牌配對 | 地圖實驗室 Pin 變為完成；`completedGames` 包含 `lab`。 |
| 圍捕小狗 | 完成一輪遊戲；成功結局會直接走結尾對話 | 成功時 `completedGames` 包含 `playground`，回地圖後操場 Pin 變為完成。 |
| 音樂教室 | 依譜面完成校歌 | 音符進度完成並顯示完成訊息。 |
| 美術教室 | 繪圖並保留作品 | 進入校長室可看到該作品。 |

重複挑戰不應重複寫入同一個 `completedGames` 項目，也不應重複跳出已取得成就的 Toast。

## 隱藏成就判定

目前共有 **6 項** 隱藏成就。每項第一次達成時，右下角應出現成就 Toast 與音效；重新達成不應重複跳 Toast。

| 成就 ID | 測試操作 | 預期結果 |
| --- | --- | --- |
| `fakerplayer` | 在入口輸入 `桓成康` 或 `校長` | 取得「I can see you」。 |
| `school-song` | 音樂教室完成整首校歌的音高順序 | 取得校歌成就。彈錯可繼續，不需從頭開始。 |
| `playground-dog` | 圍捕小狗成功 | 取得抓到小狗成就並走成功結尾對話。 |
| `garden-perfect` | 花圃完成時仍保有 5 顆心 | 取得五心完成成就。低於 5 心完成只記一般通關。 |
| `classroom-perfect` | 教室 10 題全部答對，分數 100 | 取得快問快答滿分成就。 |
| `lab-grade-a` | 實驗室結算等第為 **A** | 取得實驗紀錄校對成就。請同時確認目前規則是「A」才觸發，S 不會觸發。 |

地圖左上角的隱藏成就清單應顯示已達成項目；未達成項目維持 `？？？？？`，不可洩漏名稱或條件。

## 校長室紀念卡

### 文本分流

請以學生、教師、訪客各進入一次校長室，確認：

- 標題、開場與各段落依 `data/memorial-card.json` 的 `roleText` 使用相應文字。
- 學生 / 教師沒有覆寫的段落會回退到 `sections` 的預設文本。
- 訪客固定使用預設文本。
- `{name}`、日期、小遊戲數與成就數正確替換。

### 段落與版面

- `appendTo` 成就文字會接在指定遊戲文本的同一個 `<p>`，不能多出一個段落。
- 網頁版左頁最多 6 個 `<p>`；第 7 個起顯示在右頁。
- 左頁背景校徽、右頁美術作品與右下日期正常顯示。
- 未留下美術作品時不可顯示破圖。
- 下載紀念卡後，確認仍是雙頁設計、文字不裁切、作品與左頁校徽完整出現在圖片中。
- 取得全部 6 項成就時，確認 `all-secrets` 段落出現。

## 留言板與設定

- 便條紙可點擊；預覽應保留換行、超出內容以省略提示，完整內容在 modal 顯示。
- 閱讀留言 modal 的背景色應與被點擊便條紙相同；撰寫留言 modal 維持既有樣式。
- 留言姓名與入口玩家姓名互不覆寫。
- 留言輸入上限為 500 字，字數計數正確；送出與取消按鈕可使用。
- BGM 音量與文字速度設定在各頁一致；音樂教室進入時 BGM 靜音，離開後回復使用者設定。

## 回報格式

回報問題時請附上：

1. 測試身分：學生 / 教師 / 訪客。
2. 操作路徑與使用的輸入姓名。
3. 預期結果與實際結果。
4. 瀏覽器、螢幕尺寸或縮放比例。
5. 截圖或錄影；若涉及進度，附上 `console.table(Object.entries(localStorage))` 的結果。
